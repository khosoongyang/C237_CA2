<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
  <title>Delete Product | Seam & Soul</title>
  <style>
    /* Use Poppins font everywhere */
    body, h2, h5, p, a, button {
      font-family: 'Poppins', sans-serif;
    }

    /* Black background for navbar, white text */
    nav.navbar {
      background-color: #000 !important;
    }
    nav.navbar .navbar-brand {
      color: #fff !important;
      font-weight: 600;
    }
    nav.navbar .navbar-brand:hover {
      color: #ccc !important;
      text-decoration: none;
    }

    /* White page background */
    body {
      background-color: #fff;
      color: #000;
      min-height: 100vh;
    }

    /* Card with black border and white background */
    .card {
      border: 2px solid #000;
      background-color: #fff;
    }

    /* Headings in black with some weight */
    h2.text-danger {
      color: #000 !important;  /* override red */
      font-weight: 700;
    }

    /* Buttons */
    .btn-danger {
      background-color: #000;
      border: 2px solid #000;
      font-weight: 600;
      color: #fff;
      transition: background-color 0.3s ease;
    }
    .btn-danger:hover, .btn-danger:focus {
      background-color: #333;
      border-color: #333;
      color: #fff;
    }

    .btn-secondary {
      background-color: #fff;
      border: 2px solid #000;
      color: #000;
      font-weight: 600;
      transition: background-color 0.3s ease, color 0.3s ease;
    }
    .btn-secondary:hover, .btn-secondary:focus {
      background-color: #000;
      color: #fff;
      border-color: #000;
      text-decoration: none;
    }

    .btn-outline-secondary {
      border: 2px solid #000;
      color: #000;
      font-weight: 600;
    }
    .btn-outline-secondary:hover, .btn-outline-secondary:focus {
      background-color: #000;
      color: #fff;
      border-color: #000;
    }

    /* Alert styling */
    .alert-warning {
      background-color: #f8f9fa; /* light grey white */
      border: 2px solid #000;
      color: #000;
      font-weight: 600;
    }

    .alert-danger {
      background-color: #fff0f0;
      border: 2px solid #000;
      color: #000;
      font-weight: 600;
    }

    /* Image styling */
    img.img-thumbnail {
      border: 2px solid #000;
      border-radius: 0;
    }
  </style>
</head>
<body>
<nav class="navbar navbar-expand-sm">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Seam & Soul</a>
  </div>
</nav>

<div class="container mt-4">
  <h2 class="text-center mb-4">Delete Product</h2>

  <% if (typeof product !== 'undefined' && product) { %>
    <div class="alert alert-warning text-center">
      <strong>Are you sure you want to delete this product?</strong> This action cannot be undone.
    </div>

    <div class="card mb-4">
      <div class="card-body">
        <h5 class="card-title"><%= product.productName %></h5>
        <p class="card-text">Category: <%= product.category %></p>
        <p class="card-text">Type: <%= product.type %></p>
        <p class="card-text">Quantity: <%= product.quantity %></p>
        <p class="card-text">Price: $<%= product.price %></p>
        <p class="card-text">Description: <%= product.description %></p>

        <% if (product.image) { %>
          <p>Image:</p>
          <img src="/uploads/<%= product.image %>" class="img-thumbnail" style="max-width: 150px;" alt="<%= product.productName %>" />
        <% } else { %>
          <p class="text-muted">No image available</p>
        <% } %>
      </div>
    </div>

    <form action="/type-products/<%= product.productId %>?_method=DELETE" method="POST">
      <div class="d-flex justify-content-center gap-3">
        <button type="submit" class="btn btn-danger px-4">Yes, Delete</button>
        <a href="/" class="btn btn-secondary px-4">Cancel</a>
      </div>
    </form>
  <% } else { %>
    <div class="alert alert-danger text-center">
      <p>Product not found.</p>
    </div>
  <% } %>

  <div class="text-center mt-4">
    <a href="/" class="btn btn-outline-secondary">Back to Home</a>
  </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>