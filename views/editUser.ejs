<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet"/>
  <title>Edit User | Seam & Soul</title>
    <style>
    body {
      background: #fff;
      color: #111;
      font-family: 'Poppins', sans-serif;
    }
    .container {
      max-width: 600px;
      margin: 50px auto;
      padding: 40px;
      border-radius: 16px;
      background: #f9f9f9;
      box-shadow: 0 2px 16px rgba(0, 0, 0, 0.05);
    }
    h1 {
      text-align: center;
      margin-bottom: 30px;
      font-weight: 600;
    }
    .form-label {
      font-weight: 500;
    }
    .btn-primary {
      width: 100%;
      padding: 12px;
      font-size: 1rem;
      font-weight: 600;
    }
    .back-link {
      display: block;
      text-align: center;
      margin-top: 20px;
      text-decoration: underline;
      font-weight: 500;
      color: #111;
    }
  </style>
</head>
<body>
<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">User Management</a>
  </div>
</nav>

<div class="container mt-4">
  <h2 class="text-center mb-4">Edit User</h2>

  <% if (user) { %>
    <form action="/editUser/<%= user.userId %>" method="POST" enctype="multipart/form-data">

      <div class="mb-3">
        <label class="form-label">Username:</label>
        <input type="text" name="username" value="<%= user.username %>" class="form-control" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Email:</label>
        <input type="email" name="email" value="<%= user.email %>" class="form-control" required>
      </div>

      <div class="mb-3">
        <label class="form-label">Password:</label>
        <input type="password" name="password" placeholder="Enter new password" class="form-control">
      </div>

      <div class="mb-3">
        <label class="form-label">Current Profile Image:</label>
        <input type="text" name="currentprofilepic" value="<%= user.profilepic %>" class="form-control mb-2" readonly>
        <% if (user.profileImage) { %>
          <img src="/images/<%= user.profilepic %>" class="img-thumbnail" style="max-width:150px;">
        <% } else { %>
          <p>No image available</p>
        <% } %>
      </div>

      <div class="mb-3">
        <label class="form-label">New Profile Image (optional):</label>
        <input type="file" name="profileImage" accept="image/*" class="form-control">
      </div>

      <button type="submit" class="btn btn-primary w-100">Update User</button>
    </form>
  <% } else { %>
    <p class="text-center">No user found.</p>
  <% } %>

  <div class="text-center mt-3">
    <a href="/" class="btn btn-secondary">Back</a>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
