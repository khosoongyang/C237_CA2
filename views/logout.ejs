<!DOCTYPE html>
<html lang="en">
<head>
    <title>Logged Out | Seam & Soul</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* Body Styling */
        body {
            background: #292727; /* Dark background color */
            color: #fff; /* White text color for elements directly on body */
            font-family: 'Poppins', sans-serif; /* Global font setting */
            display: flex; /* Use flexbox for centering content */
            justify-content: center; /* Horizontally center content */
            align-items: center; /* Vertically center content */
            height: 100vh; /* Make body take full viewport height */
            margin: 0; /* Remove default body margin */
        }

        /* Styling for the logout message box */
        .logout-box {
            background: #fff; /* White background for the box */
            color: #111; /* Dark text color inside the box */
            padding: 32px; /* Padding inside the box */
            border-radius: 16px; /* Rounded corners for the box */
            box-shadow: 0 2px 16px rgba(0, 0, 0, 0.066); /* Subtle shadow for depth */
            text-align: center; /* Center align text within the box */
        }

        /* Styling for the main heading inside the logout box */
        h1 {
            font-size: 2rem; /* Font size for the heading */
            font-weight: 600; /* Semi-bold font weight */
        }

        /* Styling for the link within the logout box */
        a {
            margin-top: 16px; /* Space above the link */
            display: inline-block; /* Allows top margin and styling like a block, but takes only necessary width */
            color: #111; /* Dark text color for the link */
            text-decoration: underline; /* Underline the link */
            font-weight: 500; /* Medium font weight */
        }

        /* Hover state for the link */
        a:hover {
            color: #333; /* Slightly lighter dark color on hover */
        }
    </style>
</head>
<body>
    <div class="logout-box">
        <h1>You've been logged out.</h1>
        <p>Thank you for visiting Seam & Soul!</p>
        <a href="/login">Back to Login</a>
    </div>
</body>
</html>